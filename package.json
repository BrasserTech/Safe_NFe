{
  "name": "fiscalvault",
  "version": "1.0.0",
  "private": true,
  "description": "FiscalVault - SaaS para cofre fiscal eletronico.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,cyan \"npm --prefix backend run dev\" \"npm --prefix frontend run dev -- --host 127.0.0.1 --port 5173\"",
    "build": "npm --prefix frontend run build",
    "start:backend": "npm --prefix backend run dev",
    "start:frontend": "npm --prefix frontend run dev -- --host 127.0.0.1 --port 5173",
    "prisma:generate": "npm --prefix backend run prisma:generate"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
